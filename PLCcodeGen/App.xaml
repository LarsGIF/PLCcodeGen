<Application x:Class="PLCcodeGen.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:PLCcodeGen"
             Startup="AppStartup"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <HierarchicalDataTemplate DataType="{x:Type local:Project}" ItemsSource="{Binding CtrlArea}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <TextBlock Text="{Binding Path=PlcName}" Grid.Row="0" />
                <TextBlock Text="{Binding Path=BaseProj}" Grid.Row="0" />
            </Grid>
        </HierarchicalDataTemplate>

        <HierarchicalDataTemplate DataType="{x:Type local:Cell}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                
                <TextBlock Text="{Binding Path=PlcName}" Grid.Row="0" />
                <TextBlock Text="{Binding Path=BaseProj}" Grid.Row="0" />
            </Grid>
        </HierarchicalDataTemplate>
    </Application.Resources>
</Application>
